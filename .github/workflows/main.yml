on: 
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
      

    steps: 
      - name: 'Testing GitHub action'
        run: pwd

      - name: 'print my name'
        run: echo marwan 

