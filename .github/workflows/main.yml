on: 
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-lastest
      

    steps: 
      - name: 'Testing GitHub action'
        run: pwd
      - name: 'print my name'
        run: echo marwan 
      
